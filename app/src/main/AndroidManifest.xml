<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher23"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher23_round"
        android:supportsRtl="true"
        android:usesCleartextTraffic="true"
        tools:replace="android:fullBackupContent"
        android:theme="@style/Theme.EgyptGame"
        tools:targetApi="31">
        <activity
            android:name=".BetBooster"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.EgyptGame">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>
        <activity
            android:name=".BetBonus"
            android:configChanges="keyboardHidden|orientation|keyboard|screenLayout|screenSize|smallestScreenSize|layoutDirection"
            android:theme="@style/Theme.AppCompat.DayNight.NoActionBar"
            android:exported="true" />
        <meta-data android:name="GetApps" android:value="GetApps"/>
    </application>

</manifest>